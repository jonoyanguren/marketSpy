{
  "name": "marketspy",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "mongo:start": "npm --prefix backend run mongo:start",
    "dev": "npm --prefix front run dev",
    "build": "npm --prefix front run build",
    "preview": "npm --prefix front run preview",
    "check": "npm --prefix front run check",
    "dev:front": "npm --prefix front run dev",
    "build:front": "npm --prefix front run build",
    "check:front": "npm --prefix front run check",
    "dev:backend": "npm --prefix backend run dev",
    "build:backend": "npm --prefix backend run build",
    "check:backend": "npm --prefix backend run check"
  }
}
